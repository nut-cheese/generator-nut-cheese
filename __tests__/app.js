'use strict';
const assert = require('yeoman-assert');
// const helpers = require('yeoman-test');

describe('generator-nut-cheese:app', () => {
  // beforeAll(() => {
  //   return helpers.run(path.join(__dirname, '../generators/app'))
  //     .withPrompts({someAnswer: true});
  // });

  it('creates files', () => {
    assert.equal(2,2);
  });
});
